```bash
	# git 사용자 설정
	git config --global user.email 'git 이메일'
	git config --global user.name 'git 사용자 이름'
	
	# git 초기화 및 repository 연결
	git init
	git add .
	git commit -m 'message'
	git remote add 'repository 주소'
	git push -u origin main
```
